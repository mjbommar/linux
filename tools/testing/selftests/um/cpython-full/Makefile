# SPDX-License-Identifier: GPL-2.0
#
# um/cpython-full — full CPython stdlib regrtest under UML.
# Replaces the curated cpython-test gate with a real-coverage one
# that tracks failures explicitly via expected_failures.txt.

TEST_PROGS := run-cpython-full.sh

include ../../lib.mk
