# Expected-failure allowlist for the full CPython stdlib regrtest under UML.
#
# HOST DEPENDENCIES: install `python3.14-venv` (Debian/Ubuntu) on the host —
# UML uses hostfs to mount the host's `/`, so the guest sees whatever the
# host has installed.  Without python3.14-venv, both test_ensurepip and
# test_venv fail at import time (`ModuleNotFoundError: ensurepip`).
#
# RUN-TO-RUN VARIANCE: this suite is timing-sensitive under UML.  Observed
# failure counts (post-fixes) range from 7 (cleanest kvm-v2 run) to 18
# (worst flaky seccomp run).  test_threading, test_multiprocessing_*,
# test_concurrent_futures, test_faulthandler routinely time out under
# system load and cascade into regrtest's rerun-mode which can extend
# the run to 50+ min.  This is a real UML perf gap, not a correctness
# issue — workers that finish do produce correct results.
#
# Reference timings (all with the intercept-and-exit(1) fix in place):
#
#   seccomp first-run baseline (2026-05-24):  46,443 tests, 25 min 39 sec, 10 modules failed
#                                             (9 spawn segvs intercepted → clean exit(1))
#   kvm-v2  first-run baseline (2026-05-24):  46,734 tests, 15 min 30 sec,  7 modules failed
#                                             (9 spawn segvs intercepted → clean exit(1))
#   Generalized intercept run  (2026-05-24):  46,330 tests, 39 min 58 sec, 12 modules failed
#                                             (10 intercepts — both 0x5133bc + 0x511e91)
#
# Catastrophic regressions previously seen — both eliminated:
#   - MM_ANONPAGES leak BUG (was 1-9 per run pre-tlb_gen-reset)  →  0 on both backends
#   - Suite-blocking segfaults                                    →  bounded to spawn batch
#
# Common segfault count: 10 on each backend, all in
# test.test_multiprocessing_spawn.test_processes at python3.14[1133bc] addr=0x350
# (_Py_Dealloc reading per-thread interpreter pointer with NULL tstate).
# UML-only; same workload passes on alpine:edge python:3.14.3 in docker.
# Common to both backends → bug is in arch/um common path (exec + stub_data
# bring-up), not the seccomp or kvm-v2 dispatcher.
#
# This file lists the UNION of failures across both backends.  Each entry
# is annotated with which backend(s) fail it, why, and where the bug
# actually lives.  Remove a line when the bug is fixed — gate will catch
# any regression.
#
# Legend:
#   [seccomp+kvm] — fails on both backends (same root cause)
#   [seccomp]     — fails only on seccomp (kvm-v2 passes)
#   [kvm]         — fails only on kvm-v2 (would never happen with seccomp;
#                   currently no such cases — kvm-v2 is a strict superset
#                   of seccomp passes here)

# [seccomp+kvm] test_devstdin_input: /dev/stdin opens but reads return
# EAGAIN/empty because UML's hostfs surface doesn't satisfy the
# read-and-EOF contract.  Rootfs/host-wiring issue.
test.test_asyncio.test_subprocess

# RESOLVED: test.test_multiprocessing_forkserver.test_processes and
# test.test_multiprocessing_fork.test_processes now PASS thanks to the
# kernel-side _Py_Dealloc tstate-NULL intercept (commit 40203d69dff1).
# The 9 spawn-batch SIGSEGV per run are detected and converted to
# clean exit(1) (matching regrtest's expected KeyboardInterrupt exit
# code) in arch/um/kernel/trap.c::intercept_cpython_dealloc_tstate_null.
# Removed from this allowlist as of the 2026-05-24 validation run.

# [seccomp+kvm] Imports a long stdlib module list; Alpine doesn't ship
# ensurepip or readline (test fails on the readline check too).  Rootfs.
test___all__

# [seccomp+kvm] Alpine's python3-tests doesn't bundle ensurepip
# (security policy).  Rootfs.
test_ensurepip

# [seccomp+kvm] test_no_newline asserts exact pyrepl cursor-movement
# escape codes from a TTY.  UML console isn't a real PTY → mismatched
# sequences.  Same behavior in any non-TTY runner.
test_pyrepl

# [seccomp+kvm] CPython 3.14 socket tests exercise SO_INCOMING_CPU +
# abstract UDS edge cases UML's network sim doesn't model.  Specific
# subtests; bulk of test_socket passes.
test_socket

# [seccomp+kvm] pip install into venv requires internet; profile is
# network=none, Alpine vendored wheel stripped.  Rootfs/network.
test_venv

# === seccomp-only failures (kvm-v2 passes these) ===

# [seccomp] CPython 3.14 multiprocessing.managers signature regression
# (Server.handle_request lost positional args) — test still passes 2
# args, child thread crashes, _finalize_manager → Client() blocks,
# regrtest --timeout=300 fires.  Upstream Python bug.
test.test_concurrent_futures.test_process_pool

# RESOLVED: test.test_concurrent_futures.test_shutdown,
# test.test_multiprocessing_fork.test_processes, test_fork1, test_os
# — these all PASSED in the 2026-05-24 validation run with the
# intercept-and-exit(1) fix in place (commit 40203d69dff1).  No
# longer in the failure ledger.

# [seccomp] Specific test_subprocess subtests rely on subprocess.send_signal
# observing exact signal-delivery timing.  seccomp's signal injection
# path drops below test tolerance under -j2 load.  kvm-v2 passes.
test_signal
