# Expected-failure allowlist for the full CPython stdlib regrtest under UML
# seccomp backend, captured 2026-05-23 against arch/um HEAD with the
# init_new_context tlb_gen reset, seccomp init_thread_regs FS_BASE clear,
# and stub <-> mcontext bidirectional FS_BASE/GS_BASE sync fixes applied.
#
# Suite shape:    46,428 tests run, 2,524 skipped, 483/492 test files completed
# Kernel BUG:     0 (was 1-9 per run pre-tlb_gen-fix)
# Segfaults:      10 (all in test_multiprocessing_spawn.test_processes;
#                 confirmed UML-specific — same workload passes on Alpine
#                 edge python:3.14.3 in docker)
# Duration:       36 min 2 sec
#
# Each entry below has: module, WHY it fails, where the bug actually lives.
# Remove a line when the bug is fixed — the gate will then catch any
# regression.

# /dev/stdin device file present but no input attached; affects two
# test_devstdin_input subtests.  UML's hostfs surface of /dev/stdin
# (a symlink resolved against the launching shell's fd 0) doesn't
# satisfy CPython's read-and-EOF expectation.  Bug is in the host
# wiring, not arch/um/.
test.test_asyncio.test_subprocess

# CPython 3.14 multiprocessing.managers regression: Server.handle_request()
# signature change (no longer takes positional args) but the test still
# spawns a thread that calls it with the old signature.  TypeError →
# subsequent _finalize_manager → Client() blocks waiting for the now-
# dead server, hits regrtest's --timeout=300.
# Reproducer:  docker run python:3.14 -m test test_concurrent_futures
test.test_concurrent_futures.test_process_pool
test.test_concurrent_futures.test_shutdown

# UML-specific: under heavy parallel fork+exec (regrtest -j2 inside
# multiprocessing tests), the spawn child runs _Py_Dealloc with TLS
# slot at %fs:-0x18 reading as 0 (NULL tstate ptr).  Deterministic
# SIGSEGV @ python3.14[1133bc] addr=0x350.  NOT a FS_BASE drift —
# arch_data sync (this commit) doesn't change the signature.  Likely
# a stub-child exec-time TLS-page reset that drops a tcbhead_t field
# the new program assumes is set.  Same workload PASSes on Alpine
# edge python:3.14.3 in docker → confirmed UML-only.
test.test_multiprocessing_fork.test_processes
test.test_multiprocessing_forkserver.test_processes

# Imports a long alphabetical list of stdlib modules and checks each
# module's __all__ contents.  Some modules unavailable on Alpine
# (no ensurepip, no readline binding for pyrepl).  Root cause is the
# rootfs, not arch/um/.
test___all__

# Alpine's python3-tests doesn't ship ensurepip (security policy —
# bundled pip not allowed).  ModuleNotFoundError on import.
test_ensurepip

# Hard hang in os.fork() stress under regrtest -j2.  Doesn't segfault
# or BUG — appears to deadlock in clone3.  Killed by regrtest's
# --timeout=300.  Likely the same SMP-T-class fork race we've been
# tracking; the pool-completion roadmap may resolve.
test_fork1

# test_timerfd_TFD_TIMER_ABSTIME requires <10ms timer precision.
# UML's clocksource is itimer-backed and jitters ~10-20ms under load,
# so the 1-second timerfd fires at 0.99s.  Userspace test contract
# UML cannot meet without time_travel mode.
test_os

# Terminal output expectations: test_no_newline asserts an exact
# sequence of escape codes from pyrepl.  UML console isn't a real
# PTY, so the cursor-movement codes differ.  Not a regression — same
# behavior in any non-TTY runner.
test_pyrepl

# Specific test_subprocess subtests rely on subprocess.send_signal
# observing exact signal-delivery timing that UML's signal injection
# path doesn't preserve precisely under -j2 load.
test_signal

# CPython 3.14 socket tests exercise SO_INCOMING_CPU + abstract-
# namespace UDS edge cases that UML's network sim doesn't model
# faithfully.  Specific subtests; full test_socket suite count is
# dominated by passes.
test_socket

# pip install into venv fails because there's no internet in
# this profile (network = "none") and Alpine's vendored pip wheel
# is also stripped.  Rootfs-level issue.
test_venv
