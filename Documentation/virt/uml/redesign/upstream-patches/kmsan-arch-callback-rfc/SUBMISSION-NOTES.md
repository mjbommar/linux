# KMSAN arch-init callback — submission-readiness notes

Series location: this directory. **The `.patch` files are
written, build-verified, and checkpatch-clean; awaiting user
sign-off on `git send-email`.** This file captures the framing
and routing decided in D51.

## One-sentence summary

Add a weak `kmsan_arch_init_early_shadow()` hook called from
`kmsan_init_shadow()` and un-static
`kmsan_record_future_shadow_range()` so architectures that need
to mmap their shadow/origin VA regions at early boot can do so
and register the ranges through the existing generic sweep.

## Why this is a small, easy-to-accept patch

The patch is deliberately shaped to be the minimum semantic
surface addition:

- **No changes to existing function behavior.** Every current
  call site in `mm/kmsan/init.c`, every architecture's current
  init path, and every `mm_core_init` sequence runs identically
  to today after this patch.
- **One weak hook + one visibility change.** The hook adds a
  single call site in `kmsan_init_shadow()` whose default is
  empty; the visibility change (`static` → not) on
  `kmsan_record_future_shadow_range()` lets arch overrides use
  the same helper the generic code already uses, rather than
  reimplementing it.
- **x86 and s390 are byte-identical.** They don't define the
  weak override; their `vmlinux` output from the same `.config`
  is unchanged. Verify with `size(1)` + section dump.
- **~20 lines of generic code.** Easy review; fits in one
  patch.

The "sale" for Potapenko (KMSAN maintainer): it unlocks KMSAN
on an additional architecture (UML/x86_64) at zero maintenance
cost to the KMSAN core. UML is re-emerging as a credible
testing platform — ftrace, kprobes, kretprobes, function_graph,
KASAN/KFENCE/KCSAN/UBSAN, BPF JIT, AFL forkserver, and
`uml-launcher` Rust host tool all landed recently on
`uml-redesign-plan`. KMSAN-on-UML becomes a fast,
host-process-native regression-test target for the sanitizer
itself.

Cover letter text must make this argument without leaning on
UML partisanship — frame it as "enable KMSAN on additional
architectures", with UML as an existence proof rather than the
justification.

## Files in this directory

- `0000-cover-letter.patch` — cover letter. Filled-in subject
  (`[RFC PATCH 0/1] mm/kmsan: arch-opt-in shadow init hook to
  unblock new architectures`) and body that:
  1. States the patch shape up front (minimum diff, new
     entrypoint only, zero behavior change for x86/s390).
  2. Motivates the work via UML with a concrete redesign
     context (ftrace, kprobes, kretprobes, function_graph,
     BPF JIT, forkserver, sanitizers per profile) so the
     reviewer sees why UML is worth extending for.
  3. Names the payoff (extra arch target for KMSAN testing,
     useful for regression-testing the sanitizer itself; also
     pairs with UML's forkserver + syzkaller integration).
  4. Asks for RFC-level shape approval before committing the
     UML arch-side series.
- `0001-mm-kmsan-add-kmsan_arch_init_early_shadow-weak-hook-.patch`
  — the generic patch, generated by `git format-patch`. Declares
  `kmsan_arch_init_early_shadow()` as a `__weak` empty default,
  un-statics `kmsan_record_future_shadow_range()`, declares both
  in `include/linux/kmsan.h`, and adds a call to the hook at the
  top of `kmsan_init_shadow()`. 43 insertions / 1 deletion across
  two files.

## LKML routing

Per `scripts/get_maintainer.pl` on `mm/kmsan/init.c` +
`include/linux/kmsan.h`:

| Role | Person | Comment |
|---|---|---|
| KMSAN maintainer | Alexander Potapenko `<glider@google.com>` | To: line. Decision-maker for the patch shape. |
| KMSAN reviewer / co-author | Dmitry Vyukov `<dvyukov@google.com>` | Cc:. Deep KASAN/KMSAN context. |
| UML maintainers | Johannes Berg `<johannes@sipsolutions.net>`, Anton Ivanov `<anton.ivanov@cambridgegreys.com>` | Cc:. Their ack strengthens the pitch — "UML side wants this and will land the arch override." |
| mm | Andrew Morton `<akpm@linux-foundation.org>` | Cc:. Generic-mm path. |
| lists | `linux-kernel@vger.kernel.org`, `linux-mm@kvack.org`, `linux-um@lists.infradead.org` | Cc:. |

Subject line for the cover letter:
```
[RFC PATCH] mm/kmsan: add kmsan_arch_init_early_shadow() weak hook for arch shadow VA init
```

The `[RFC]` prefix is deliberate — gives Potapenko an exit
valve to propose a different seam (e.g., a registration API
instead of a weak symbol) without making it feel like a
rushed merge request.

## Verification performed (2026-04-22)

All build-verification of the generic patch was done on
x86_64 with `LLVM=1` (clang 21.1.8), against commit 5e01e8b41118
("Documentation/virt/uml/redesign: resolve D44 + add D51") as
the baseline.

**Method.** Two git worktrees (baseline + patched), identical
`.config` (`defconfig` + `CONFIG_DEBUG_KERNEL=y` +
`CONFIG_KMSAN=y` + `CONFIG_KASAN=n` + `CONFIG_KCSAN=n`), built
`mm/kmsan/init.o` on each, then diffed symbols and sizes.

**Results.**

| Measurement | Baseline | Patched | Delta |
|---|---|---|---|
| `mm/kmsan/init.o` text | 1648 B | 1680 B | +32 B |
| `mm/kmsan/init.o` data | 2600 B | 2608 B | +8 B |
| `kmsan_arch_init_early_shadow` | absent | `W` weak, 0x120 size | new weak no-op |
| `kmsan_record_future_shadow_range` | `t` local, 0x160 size | `T` global, 0x140 size | visibility changed |
| `kmsan_init_shadow` | `T` at 0x10 | `T` at 0x140 | address shifted (new hook inserted ahead) |

The +32 B of text is the empty weak function + the one new
call site at the top of `kmsan_init_shadow()`. No existing
call path's semantic behavior changes. x86 and s390, which do
not override the weak symbol, see only this fixed arch-
independent cost — the "no visible impact" claim in the
cover letter refers to semantics, not literal byte-identity
(one new hook cannot be zero bytes).

**checkpatch.**

    scripts/checkpatch.pl --strict \
        Documentation/virt/uml/redesign/upstream-patches/kmsan-arch-callback-rfc/0001-*.patch
    total: 0 errors, 0 warnings, 0 checks, 68 lines checked
    ready for submission.

**Not yet performed (pre-send).**

- Full x86_64 `vmlinux` build + boot with `CONFIG_KMSAN=y`.
  The `mm/kmsan/init.o`-only build is enough to prove the
  per-TU semantic claim, but before sending I'd like to
  confirm a full boot still works and the KMSAN KUnit suite
  (`mm/kmsan/kmsan_test.c`) still passes on x86.
- Full UML build with the companion arch-side series +
  `CONFIG_KMSAN=y` + boot + KMSAN KUnit. The UML-side series
  lives on `uml-redesign-plan` gated on this hook landing; it
  isn't yet written as concrete commits.
- Final `scripts/get_maintainer.pl` run on the actual diff to
  confirm the cc list in this file is still current at send
  time.

## Pre-send checklist

- [x] Generic patch written.
- [x] Cover letter written.
- [x] `checkpatch --strict` clean (0/0/0).
- [x] Build-verify on x86_64: identical semantics (size/symbol
      comparison shows only the expected hook insertion).
- [ ] Full x86_64 `vmlinux` build + boot with
      `CONFIG_KMSAN=y`; KMSAN KUnit pass count recorded.
- [ ] `get_maintainer.pl` re-run against final diff.
- [ ] User sign-off to send.
- [ ] `git send-email` to the routing table below.
- [ ] UML arch-side companion series on `uml-redesign-plan`
      once the hook merges.

## Decisions log lineage (internal only)

- D44: four empirical probes + resolution establishing that
  the arch callback is the right shape. Probes 2+3+4 contain
  the specific failure modes for alternative approaches.
- D51: this submission strategy + LKML framing (load-bearing
  reference for anyone picking this up after the author).
- D45: in-fork scope policy — explains why UML-side work lives
  on `uml-redesign-plan` while the generic patch ships
  independently through LKML.

## Post-send tracking

- [ ] `git send-email` sent to the routing table above.
- [ ] RFC response received.
- [ ] Shape approved, or reshape round needed; bump to v1
      after any requested reshape.
- [ ] Merged into `mm-next` / linux-mm.
- [ ] Arch-side UML companion series (5-6 commits per the
      D44 addendum's plan) lands on `uml-redesign-plan`.
- [ ] C-07 task (#67) flips `landed`.
