# SPDX-License-Identifier: GPL-2.0
#
# Shared backend helpers (used by ptrace and seccomp; KVM has its own).
# Currently a placeholder; populated as A-02 extracts shared code from
# arch/um/{kernel,os-Linux}/skas/.

obj-y += placeholder.o
