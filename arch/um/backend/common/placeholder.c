// SPDX-License-Identifier: GPL-2.0
/*
 * Placeholder TU for arch/um/backend/common/.
 *
 * Real shared code lands here as A-02 extracts switch_threads,
 * new_thread, start_idle_thread, halt_skas, reboot_skas, and the
 * timer-mode dispatch from arch/um/os-Linux/skas/process.c into
 * backend-shared form. Until then this file ensures the directory
 * compiles cleanly so the backend/ Makefile chain works.
 */
