# SPDX-License-Identifier: GPL-2.0
#
# Backend conformance test suite (workstream A-05).
#
# Wired in via CONFIG_UM_BACKEND_CONTRACT_TEST. The full suite has one
# kunit test per op plus cross-backend equivalence tests; the A-02 first
# slice ships a skeleton with one test for read_persistent_clock_ns.

obj-$(CONFIG_UM_BACKEND_CONTRACT_TEST) += test_ops.o
