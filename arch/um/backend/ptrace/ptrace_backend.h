/* SPDX-License-Identifier: GPL-2.0 */
/*
 * Internal header for arch/um/backend/ptrace/.
 *
 * The ops table struct, dispatch macro, and per-backend op
 * prototypes are all in <backend.h> (shared header). This header
 * is just a stable include point for ptrace TUs.
 */
#ifndef __ARCH_UM_BACKEND_PTRACE_H
#define __ARCH_UM_BACKEND_PTRACE_H

#include <backend.h>

#endif /* __ARCH_UM_BACKEND_PTRACE_H */
